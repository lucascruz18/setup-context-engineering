# AGENTS.md - {{PROJECT}}

> Source of truth for AI agents working in this repo.
> Generated by the setup-context-engineering skill; edit freely - human content is preserved on re-run.

## Stack & Domain
[3-5 lines: languages, frameworks, what the system does]

## Required Behavior
- Read `memory/tasks/` and identify the task type before coding.
- Load the matching rules (see "Context by Task Type").
- Run lint + typecheck before declaring a task done.
- Log non-obvious decisions in `memory/docs/decisions.md`.

## Context by Task Type
- Backend task  -> .claude/rules/shared/* + .claude/rules/backend/*
                 + .claude/prompts/{api-contracts,error-handling,logging,structured-outputs,task-spec}.md
- Frontend task -> .claude/rules/shared/* + .claude/rules/frontend/*
                 + .claude/prompts/{api-client,logging,structured-outputs,task-spec}.md

(omit sides that do not exist)

## Never Do This
[derived from observed anti-patterns; each item cites path:line]
-

## Structure
[real map: roots, layers]

## Commands
```bash
lint:      {{LINT_CMD}}
typecheck: {{TYPECHECK_CMD}}
test:      {{TEST_FULL_CMD}}
build:     {{BUILD_CMD}}
```

## Memory & Specs
- `memory/docs/discovery.md` - observed patterns snapshot
- `memory/docs/decisions.md` - append-only decision log
- `memory/docs/TESTING.md` - coverage matrix + gate commands
- `memory/STATE.md` - functional state across sessions
- `memory/tasks/` - atomic task specs
- `specs/` - feature specs

## Local Skills
- `.claude/skills/pr-review/SKILL.md` - multi-agent PR reviewer
